# nlp-summarizer-emotions
an NLP system that combines emotion classification and summarization by fine-tuning DistilRoBERTa on the Kaggle GoEmotions dataset and pairing it with a BART summarizer. Built a preprocessing pipeline for generating summaries and JSON-formatted emotion outputs for any input passage.
